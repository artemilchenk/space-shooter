export enum EBoardRegisteredKeys {
  LEFT = "ArrowLeft",
  RIGHT = "ArrowRight",
  SPACE = "Space",
}

export enum EntityTypes {
  HERO = "hero",
  ASTEROID = "asteroid",
  BULLET = "bullet",
  BOSS = "boss",
}
